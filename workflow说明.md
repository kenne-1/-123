# workflow 说明

## 目标

用一个轻量本地 demo 展示 AI 辅助热点洞察与趋势决策的完整链路。重点不是复杂模型，而是把内容运营里的判断步骤拆成可复用、可解释、可导出的工作流。

## 1. 热点监测

输入数据来自 `sample_hotspot_data.csv`。字段包括日期、平台、模拟品牌、文本、点赞、评论、收藏和品类。实际业务中这一层可以替换为合规的数据看板导出文件；本 demo 不连接真实平台。

## 2. 数据清洗

清洗逻辑包括：

- 去除 URL
- 去除 @提及
- 去除特殊符号
- 去除纯数字
- 合并空白字符

清洗后的文本用于后续候选短语提取。

## 3. 候选短语提取

系统使用中文字符 2-gram / 3-gram 方式提取短语，并过滤“爆款、好物、推荐、链接、官方”等泛化营销词。

这样做的好处是实现简单、无需复杂分词词典，适合作品集 demo 展示。限制是语义完整度有限，所以页面也会给出“需人工复核”的风险提示。

## 4. 候选热梗识别

每个候选短语会计算：

- `freq_recent`：近 7 天出现频次
- `freq_prev`：前 7 天出现频次
- `growth_rate`：相对增长率
- `brand_coverage`：出现该短语的模拟品牌数
- `platform_coverage`：出现该短语的平台数
- `pmi_like_score`：简化凝固度分数
- `engagement_score`：基于点赞、评论、收藏加权的互动强度
- `usability_score`：综合可用性评分

其中 `pmi_like_score` 使用短语频率和单字频率的相对关系估算“语言凝固度”，用于识别更像固定表达的候选短语。

## 5. 生命周期判断

规则如下：

- 潜力期：低频但增长率高，且有一定品牌覆盖
- 爆发中：高频且持续增长
- 衰减期：近 7 天频次低于前 7 天
- 低热观察：频次低且增长不明显

该规则便于解释，也方便在面试中说明“业务判断如何被产品化”。

## 6. 热点等级

系统根据 `usability_score` 和生命周期输出：

- 夯：强烈推荐，适合直接跟进
- 人上人：表现不错，建议小范围试水
- NPC：普通，继续观察
- 拉完了：热度或可用性较差，不建议使用

同时输出风险提示，例如热度衰减、语义不完整、跨平台扩散不足、可能与品牌调性不匹配等。

## 7. AI Prompt 生成

系统不会在本地直接调用大模型，而是把当前候选词条的判断依据整理成一段可复制的 Prompt，交给 Kimi、通义或 ChatGPT 继续生成内容策略。Prompt 包含：

- 候选短语和频次变化
- 增长率、品牌覆盖和平台覆盖
- 生命周期、热点等级和风险提示
- 要求大模型输出的分析结构

## 8. 报告导出

页面支持导出：

- 候选结果 CSV
- Markdown 报告

报告包含 Top 10 候选热梗、生命周期、热点等级和后续 AI 分析提示。
